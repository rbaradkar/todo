import Realm from "realm";


// Invokes the shared instance of the Realm app.
// TODO: Create a Realm App instance with your Realm app ID.
// const app = new Realm.App({id: "<your Realm app ID here>"}); // Set Realm app ID here.
const app = new Realm.App({id: "to-do-udwrf"}); // Set Realm app ID here.

export default app;

